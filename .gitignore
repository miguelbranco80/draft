# Out-of-tree CMake builds and host-specific editor metadata are derived state.
build/
build-*/
cmake-build-*/
.DS_Store

# Resolution manifests, generated Draft source, and evidence are intentional
# project inputs. Only disposable elaborator/compiler intermediates are ignored.
.draft/build/
.draft/tmp/
