package c_interop

foreign libc {
    labs :: c proc(value: i64) -> i64
}

export increment as "draft_increment" :: c proc(value: i32) -> i32 {
    return value + 1
}

main :: proc() {
    assert(labs(-42) == 42)
    assert(increment(41) == 42)
}
