// Option is the conventional optional-value variant. It owns no storage beyond
// its inline discriminator and payload, and has no compiler-defined behavior:
// zero initialization selects none, while switch payload binding unwraps some.
// Draft specification: section 5, "Variants, Option, and Result".
package option

pub Option[T: type] :: variant {
    none,
    some: T,
}
