package result

// Result keeps success and failure types explicit.  The first alternative is
// the zero value, so callers should choose T's zero value deliberately when a
// default-initialized Result is meaningful.
pub Result[T: type, E: type] :: union {
    success: T,
    failure: E,
}
