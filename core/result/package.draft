package result

// Result keeps failure and success types explicit. The first alternative is
// the zero value, so a default-initialized Result is always an error.
pub Result[T: type, E: type] :: union {
    err: E,
    ok: T,
}
