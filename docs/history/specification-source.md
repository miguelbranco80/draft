# Specification source history

The pre-reorganization README described the split Draft 1 revision-2
specification as preserving a monolithic source at
`../agent-native-systems-language-v2.md`. That file was not present in this
workspace when the documentation was reorganized. The checked-in split edition
under `docs/specification/` is therefore the repository's canonical copy.
This note preserves the former reference without retaining a broken link.
